apiVersion: v1
kind: Service
metadata:
  name: location
  labels:
    app: location
spec:
  selector:
    app: location
  ports:
    - name: http
      port: 8086
      targetPort: http
      protocol: TCP
  type: ClusterIP
