apiVersion: v1
kind: Service
metadata:
  name: location
  labels:
    app: location
spec:
  selector:
    app: location
  ports:
    - port: 8086
      targetPort: 8086
      protocol: TCP
  type: ClusterIP
