apiVersion: v1
kind: Service
metadata:
  name: webhook
  labels:
    app: webhook
spec:
  selector:
    app: webhook
  ports:
    - name: http
      port: 8092
      targetPort: http
      protocol: TCP
  type: ClusterIP
