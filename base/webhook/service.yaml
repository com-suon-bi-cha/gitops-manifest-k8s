apiVersion: v1
kind: Service
metadata:
  name: webhook
  labels:
    app: webhook
spec:
  selector:
    app: webhook
  ports:
    - port: 8092
      targetPort: 8092
      protocol: TCP
  type: ClusterIP
