apiVersion: v1
kind: Service
metadata:
  name: media
  labels:
    app: media
spec:
  selector:
    app: media
  ports:
    - name: http
      port: 8083
      targetPort: http
      protocol: TCP
  type: ClusterIP
