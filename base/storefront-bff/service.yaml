apiVersion: v1
kind: Service
metadata:
  name: storefront-bff
  labels:
    app: storefront-bff
spec:
  selector:
    app: storefront-bff
  ports:
    - port: 8087
      targetPort: 8087
      protocol: TCP
  type: ClusterIP
