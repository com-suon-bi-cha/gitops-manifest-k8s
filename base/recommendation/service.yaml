apiVersion: v1
kind: Service
metadata:
  name: recommendation
  labels:
    app: recommendation
spec:
  selector:
    app: recommendation
  ports:
    - port: 8095
      targetPort: 8095
      protocol: TCP
  type: ClusterIP
