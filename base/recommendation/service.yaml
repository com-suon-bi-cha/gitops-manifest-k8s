apiVersion: v1
kind: Service
metadata:
  name: recommendation
  labels:
    app: recommendation
spec:
  selector:
    app: recommendation
  ports:
    - name: http
      port: 8095
      targetPort: http
      protocol: TCP
  type: ClusterIP
