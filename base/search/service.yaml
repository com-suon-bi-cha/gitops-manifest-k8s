apiVersion: v1
kind: Service
metadata:
  name: search
  labels:
    app: search
spec:
  selector:
    app: search
  ports:
    - name: http
      port: 8092
      targetPort: http
      protocol: TCP
  type: ClusterIP
