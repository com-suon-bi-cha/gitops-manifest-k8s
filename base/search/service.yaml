apiVersion: v1
kind: Service
metadata:
  name: search
  labels:
    app: search
spec:
  selector:
    app: search
  ports:
    - port: 8092
      targetPort: 8092
      protocol: TCP
  type: ClusterIP
