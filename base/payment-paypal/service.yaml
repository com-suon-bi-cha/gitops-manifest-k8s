apiVersion: v1
kind: Service
metadata:
  name: payment-paypal
  labels:
    app: payment-paypal
spec:
  selector:
    app: payment-paypal
  ports:
    - port: 8093
      targetPort: 8093
      protocol: TCP
  type: ClusterIP
