apiVersion: v1
kind: Service
metadata:
  name: sampledata
  labels:
    app: sampledata
spec:
  selector:
    app: sampledata
  ports:
    - port: 8094
      targetPort: 8094
      protocol: TCP
  type: ClusterIP
