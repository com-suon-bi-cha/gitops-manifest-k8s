apiVersion: v1
kind: Service
metadata:
  name: cart
  labels:
    app: cart
spec:
  selector:
    app: cart
  ports:
    - name: http
      port: 8084
      targetPort: http
      protocol: TCP
  type: ClusterIP
