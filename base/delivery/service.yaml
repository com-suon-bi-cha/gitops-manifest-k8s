apiVersion: v1
kind: Service
metadata:
  name: delivery
  labels:
    app: delivery
spec:
  selector:
    app: delivery
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
