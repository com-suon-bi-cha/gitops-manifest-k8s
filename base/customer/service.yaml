apiVersion: v1
kind: Service
metadata:
  name: customer
  labels:
    app: customer
spec:
  selector:
    app: customer
  ports:
    - name: http
      port: 8088
      targetPort: http
      protocol: TCP
  type: ClusterIP
