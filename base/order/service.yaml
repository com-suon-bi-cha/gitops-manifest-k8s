apiVersion: v1
kind: Service
metadata:
  name: order
  labels:
    app: order
spec:
  selector:
    app: order
  ports:
    - name: http
      port: 8085
      targetPort: http
      protocol: TCP
  type: ClusterIP
