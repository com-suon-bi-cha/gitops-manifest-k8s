apiVersion: v1
kind: Service
metadata:
  name: backoffice-ui
  labels:
    app: backoffice-ui
spec:
  selector:
    app: backoffice-ui
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
