apiVersion: apps/v1
kind: Deployment
metadata:
  name: tax
spec:
  template:
    spec:
      containers:
        - name: tax
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres-postgresql.dev.svc.cluster.local:5432/tax?sslmode=disable"
