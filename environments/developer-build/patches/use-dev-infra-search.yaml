apiVersion: apps/v1
kind: Deployment
metadata:
  name: search
spec:
  template:
    spec:
      containers:
        - name: search
          env:
            - name: ELASTICSEARCH_URL
              value: "elasticsearch.dev.svc.cluster.local:9200"
